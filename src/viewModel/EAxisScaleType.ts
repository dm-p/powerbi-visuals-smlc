/**
 * Used to specify the type of axis within the visual so that we can code its behaviour correctly downstream.
 */
enum EAxisScaleType {
    Linear = 0,
    Time = 1,
    Point = 3
}

export default EAxisScaleType;
