/**
 * Provides placement details for margins and spacing within the visual, where needed.
 */
export default interface IElementSideDimension {
  top: number;
  bottom: number;
  left: number;
  right: number;
}
