/**
 * Specifies how small multiple layout column calculation should be carried out, and ergo, the grid.
 */
export enum HorizontalGridMode {
  width = "width",
  column = "column"
}

/**
 * Specifies how row height, relative to the grid dimensions.
 */
export enum VerticalGridMode {
  height = "height",
  fit = "fit"
}
